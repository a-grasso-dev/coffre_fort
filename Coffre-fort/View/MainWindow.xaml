<Window x:Class="Coffre_fort.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Coffre_fort"
        mc:Ignorable="d"
        Title="Coffre_Fort" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBox x:Name="SearchBox" Width="200" Height="30" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Rechercher" Width="100" Height="30" Command="{Binding SearchCommand}"/>
            <Button Content="Ajouter" Width="100" Height="30" Command="{Binding AddCommand}"/>
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding Passwords}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding NomCompte}" FontWeight="Bold" Width="150"/>
                        <TextBlock Text="{Binding MotDePasseDechiffre}" Width="200"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
